const getters = {
    // device: state => state.app.device,
    avatar: state => state.breed.avatar,
    name_breed: state => state.breed.name,
    errorLogs: state => state.errorLog.logs
  }
  export default getters